# 프로젝트 현황 분석 TODO

> 작성일시: 2026-08-10 11:22 (KST)  
> 기준: 현재 `master` 브랜치 / 커밋 `f19981f`  
> 상태: 요구사항·ERD는 폭넓게 설계되어 있으나, 구현은 구매 MVP 단계이다.

## 현재 구현 범위

- 구매자 앱: 회원가입·로그인, 상품 조회, 장바구니, 수기 배송지 입력 주문, 주문내역
- 관리자 웹: 로그인, 대시보드, 주문 상태 수정, 상품 가격·재고·상태 수정
- 백엔드: JWT 인증, 상품/장바구니/주문 API, 개발용 결제 완료 처리, 재고 이력
- 데이터베이스: 55개 테이블의 ERD·Flyway 마이그레이션 구성 완료

## 우선순위 TODO

### P0 — 거래 안전성

- [ ] **실결제 연동 전 주문 확정 방지**
  - 현재 주문 API 호출만으로 결제 완료, 재고 차감, 장바구니 비움이 실행된다.
  - PG 결제 승인 성공 콜백 또는 개발용 결제 모드가 확인된 경우에만 주문을 결제 완료로 전환한다.
  - 결제 실패·취소·중복 콜백 처리 기준을 함께 정의한다.

- [x] **주문 중복 요청 방지(Idempotency) 적용** — 2026-08-10 완료
  - `Idempotency-Key` 헤더 + `orders.idempotency_key` 컬럼 + `(account_id, idempotency_key)` 부분 유니크 인덱스(`V5`).
  - 앱은 주문서 화면 진입 시 키를 한 번 생성해 재시도 내내 유지한다(주문서에 새로 들어오면 새 키).
  - 동시 중복 요청은 유니크 제약에 걸린 쪽이 먼저 만들어진 주문을 재조회해 **같은 주문을 반환**한다
    (`OrderCheckoutService` — 복구 재조회는 트랜잭션 밖에서 해야 해서 별도 서비스로 분리).
  - 검증: 결제 버튼 3연타 → 서버에 POST 3건 도달했지만 주문 1건·재고차감 1건. 키 없이 보내면 종전대로 새 주문 생성(하위 호환).

### P1 — 재고·주문 상태

- [x] **동시 주문 재고 정합성 보장** — 2026-08-10 완료 (자동화 테스트는 미완, 아래 참고)
  - **비관적 락** 채택(`ProductRepository.findForStockUpdate`, `PESSIMISTIC_WRITE`). 낙관적 락은 인기 상품에
    경합이 몰리면 재시도가 계속 실패해 사용자 경험이 나빠져서 제외했다.
  - 데드락 방지를 위해 장바구니 항목을 **상품 ID 순으로 정렬해 잠근다**(두 주문이 같은 상품들을 반대 순서로
    잠그는 상황을 없앰).
  - 검증: 재고 1개 상품에 4명 동시 주문 → 수정 전 **4건 전부 성공(초과판매)**, 수정 후 1건 성공 / 3건 409.
  - 남은 것: 위 검증은 실제 서버에 동시 요청을 쏘는 스크립트로 했다. 자동화 테스트로 만들려면
    Testcontainers 구성이 필요해서 "핵심 기능 테스트 추가" 항목과 함께 처리한다.

- [ ] **옵션 SKU 구매 흐름 구현**
  - 장바구니에 `optionCombinationId`를 받을 수 있게 한다.
  - 옵션 조합별 판매 상태, 재고, 추가 가격을 검증·차감·스냅샷한다.
  - 옵션 없는 상품은 기존 `products.price`, `products.stock_quantity`를 사용한다.

- [ ] **주문 상태 전이 규칙 도입**
  - 단순 상태 PATCH 대신 허용된 전이만 수행하도록 한다.
  - 예: 결제완료 → 상품준비중 → 배송중 → 배송완료, 결제완료/상품준비중 → 취소.
  - 취소/반품/환불 시 결제·환불·재고·배송 상태를 같은 트랜잭션 규칙으로 연결한다.

- [ ] **배송비 정책 보완**
  - 도서산간 추가 배송비와 회사별 조건을 주문서 단계에서 안내한다.
  - 주문 확정 시점의 배송비 스냅샷을 유지한다.

### P1 — 인증·권한

- [ ] **토큰 갱신 및 폐기 구현**
  - refresh token 갱신 API와 로그아웃/강제 만료 전략을 구현한다.
  - 계정 정지, 관리자 권한 회수 시 기존 access token의 처리 기준을 정한다.

- [ ] **운영 CORS 정책 분리**
  - 개발/운영 프로필을 분리한다.
  - 운영 환경에서는 허용할 앱·웹 Origin만 환경변수로 지정한다.

- [ ] **세분화된 관리자·회사 권한 적용**
  - 현재는 `ADMIN` 역할 하나로 모든 관리자 API에 접근한다.
  - ERD의 `admin_permissions`, 회사 역할·권한을 실제 API 인가 규칙으로 연결한다.

- [ ] **웹 토큰 보관 방식 재검토**
  - 관리자 웹과 모바일 웹은 현재 localStorage에 토큰을 저장한다.
  - 운영 웹은 HttpOnly/Secure 쿠키 기반 세션 또는 XSS 완화 정책을 결정한다.

### P1 — 다음 기능 묶음

- [ ] **상품 등록 및 이미지 업로드**
  - 관리자 상품 신규 등록 화면/API를 구현한다.
  - `files`, `product_files`에 저장하고 대표 이미지·정렬을 관리한다.
  - 업로드 전 MIME type, 확장자, 실제 파일 내용, 크기를 검증한다.

- [ ] **쿠폰 적용**
  - 공용 코드 입력형과 계정 발급형을 지원한다.
  - 사용 가능 기간, 최소 주문금액, 대상 상품/카테고리/회사, 계정당 사용 횟수를 검증한다.
  - 주문 금액·쿠폰 사용 상태를 하나의 트랜잭션으로 확정한다.

- [ ] **환불 처리**
  - `order_claims` 승인액과 `refunds` 실제 처리 원장을 분리해 사용한다.
  - 부분 환불 합계가 원결제 금액을 초과하지 않도록 검증한다.
  - 환불 완료된 금액만 관리자 순매출에서 차감한다.

- [ ] **리뷰 및 위시리스트**
  - 구매한 주문 항목에 대해서만 리뷰를 작성할 수 있게 한다.
  - 중복 리뷰, 삭제/재작성, 찜 해제 후 재찜을 처리한다.

### P2 — 주소·조회 품질

- [ ] **계정 주소록 CRUD 구현**
  - 주소 별칭(우리집, 사무실 등), 기본 배송지를 관리한다.
  - 주문 시 선택한 주소를 주문 스냅샷으로 복사한다.
  - `addressId`를 전달받으면 반드시 현재 계정 소유 주소인지 검증한다.

- [ ] **목록 조회 성능 개선**
  - 관리자 주문 목록의 주문자·주문 항목 개별 조회(N+1)를 projection/join으로 줄인다.
  - 구매자 주문내역도 페이지네이션을 적용한다.

- [ ] **통합 오류 처리 보강**
  - DB 유니크 제약 위반, 낙관적 락 충돌 등을 500이 아닌 API 계약 오류로 변환한다.

### P2 — 테스트·운영

- [ ] **핵심 기능 테스트 추가**
  - 현재 백엔드 자동 테스트는 Spring context 기동 1건뿐이다.
  - 우선 대상: 로그인, 권한, 장바구니, 재고 차감, 중복 주문, 주문 상태 전이, 환불 합계.
  - 사용자 작업 원칙: 평소에는 변경 기능 단위 테스트만 실행하고, 요청 시 기능/통합 테스트를 실행한다.

- [ ] **CI 구성**
  - GitHub Actions 또는 Jenkins Pipeline으로 백엔드 컴파일·단위 테스트, 웹/모바일 타입 검사·린트를 자동화한다.

- [ ] **앱 API 주소 환경화**
  - 현재 모바일 API 주소는 localhost/Android 에뮬레이터 주소로 고정되어 있다.
  - Expo 환경 변수로 개발·스테이징·운영 API 주소를 분리한다.

## 권장 구현 순서

1. 주문 중복 방지와 동시 재고 처리
2. 상품 등록·이미지 업로드
3. 옵션 SKU 구매
4. 쿠폰 적용
5. 클레임·환불
6. 리뷰·위시리스트
7. 주소록, 권한, 테스트·CI 보강
