# PostgreSQL
POSTGRES_DB=shopping_mall
POSTGRES_USER=shopping_admin
POSTGRES_PASSWORD=change_me
POSTGRES_PORT=5432

# Jenkins
JENKINS_PORT=8080
JENKINS_AGENT_PORT=50000
# GID of the "docker" group on the host — run `getent group docker` to find it
DOCKER_GID=983
